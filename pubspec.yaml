name: efectiva_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter 
  
  # UI
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.9
  shimmer: ^3.0.0
  
  # Estado
  provider: ^6.1.1
  
  # HTTP
  http: ^1.2.1
  
  # Local storage
  shared_preferences: ^2.2.2
  
  # Biometría
  local_auth: ^2.1.6
  
  # Útiles
  intl: ^0.18.1
  image_picker: ^1.0.5
  flutter_dotenv: ^5.2.1

  font_awesome_flutter: ^10.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
    - assets/images/icono_efectiva.png
